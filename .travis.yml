language: python
python:
    - "2.7.12"
# Install all dependencies
install:
    - pip install -r requirements.txt
    - pip install psycopg2
# command to run tests
script: pytest
after_success:
    - coveralls